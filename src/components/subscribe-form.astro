---
---
<div class="mt-12 w-full max-w-xl border border-zinc-700 rounded-lg p-6 bg-zinc-900/50">
  <h2 class="text-lg font-semibold text-zinc-200 mb-2">Stay in the loop</h2>
  <p class="text-sm text-zinc-400 mb-4">Get new posts delivered to your inbox.</p>

  <form name="subscribe" method="POST" netlify class="flex gap-3 w-full">
    <input type="hidden" name="form-name" value="subscribe" />

    <input
      type="email"
      name="email"
      required
      placeholder="you@example.com"
      class="flex-1 rounded-md border border-zinc-700 bg-zinc-800 px-4 py-2 text-sm text-white placeholder-zinc-500 focus:outline-none focus:ring-2 focus:ring-blue-500"
    />

    <button
      type="submit"
      class="rounded-md bg-zinc-100 text-black px-4 py-2 text-sm font-medium hover:bg-white transition whitespace-nowrap"
    >
      Subscribe →
    </button>
  </form>
</div>
